'use strict';

/* Sets Services */

var setsisntanceServices = angular.module('setsinstanceServices', ['ngResource']);

setsServices.factory('SetsInstance', [ '$resource', function($resource) {
	/*return $resource('../api/sets', {}, {
		query: {
			method: 'GET',
			isArray: true
		}
	});*/
}]);